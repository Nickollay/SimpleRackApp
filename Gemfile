source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.7.2'

gem 'rackup'
gem 'puma'

group :development, :test do
  gem 'pry'
  gem 'pry-remote'
  gem 'pry-nav'
end
